# tools/

Deterministic Python scripts. Each script does one thing: an API call, a data transformation, a DB query, a file operation.

- One script per task
- No business logic — that lives in workflows
- Credentials come from `.env`
