# Gmail / Google Drive (OAuth)
# credentials.json and token.json are generated via Google OAuth flow

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# PostHog
POSTHOG_API_KEY=
POSTHOG_PROJECT_ID=

# BigQuery
GOOGLE_APPLICATION_CREDENTIALS=  # path to service account JSON

# Add other keys as integrations are built out
