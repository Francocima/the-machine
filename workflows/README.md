# workflows/

Markdown SOPs. Each workflow defines:

- **Objective** — what this automation does
- **Inputs** — what's required to run it
- **Steps** — which tools to call and in what order
- **Outputs** — what gets produced and where it goes
- **Edge cases** — known failure modes and how to handle them

Update workflows when you discover better methods, rate limits, or recurring issues.
